const initialState = {
  quizies: [],
  loading: true,
};

export default function quizReduer(state = initialState, action) {
  switch (action.type) {
    case value:
      break;

    default:
      return state;
  }
}
